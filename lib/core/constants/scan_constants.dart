class ScanConstants {
  /// Confidence threshold below which a scan result is considered low confidence
  /// and requires manual genus correction before proceeding.
  /// Value is a percentage (0–100).
  static const double lowConfidenceThreshold = 70.0;
}
